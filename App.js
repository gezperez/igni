import React from 'react';
import {SafeAreaView} from 'react-native';
import Config from 'react-native-config';

const App = () => {
  console.log(Config);
  return <SafeAreaView />;
};

export default App;
